==Installation

  cd ~/
  git clone git@github.com:mateuszzawisza/dotfiles.git .vim

  ln -s .vim/vimrc .vimrc 
  ln -s .vim/gvimrc .gvimrc 

To update bundles

  cd ~/.vim
  git submodule init
  git submodule update


